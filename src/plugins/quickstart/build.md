# Building a Plugin

Now, run the following terminal command from the plugin package directory:

```bash
pnpm build
```

A `plugin_package.zip` file will be created in the `/dist` directory.

In Caido, **click** on the `Install Package` button in the **Plugins** interface and select this file to install the plugin.